rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
  
    // For simplicity in this prototype, allow read/write.
    // In a production app, you would have more granular rules.
    match /{document=**} {
      allow read, write: if true; 
    }
  }
}
